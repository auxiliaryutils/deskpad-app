package com.example.deskpad.core

/** Framework-free geometry types shared by the pure logic units. */
data class Bounds(val width: Float, val height: Float)
data class Point(val x: Float, val y: Float)
